server:
  port: 8093
spring:
  application:
    name: "card"
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/card
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
